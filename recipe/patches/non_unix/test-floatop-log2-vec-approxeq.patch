--- a/test/behavior/floatop.zig
+++ b/test/behavior/floatop.zig
@@ -976,10 +976,10 @@
     var v: @Vector(4, f32) = [_]f32{ 1.1, 2.2, 0.3, 0.4 };
     _ = &v;
     const result = @log2(v);
-    try expect(@log2(@as(f32, 1.1)) == result[0]);
-    try expect(@log2(@as(f32, 2.2)) == result[1]);
-    try expect(@log2(@as(f32, 0.3)) == result[2]);
-    try expect(@log2(@as(f32, 0.4)) == result[3]);
+    try std.testing.expectApproxEqRel(@log2(@as(f32, 1.1)), result[0], comptime std.math.floatEps(f32) * 4);
+    try std.testing.expectApproxEqRel(@log2(@as(f32, 2.2)), result[1], comptime std.math.floatEps(f32) * 4);
+    try std.testing.expectApproxEqRel(@log2(@as(f32, 0.3)), result[2], comptime std.math.floatEps(f32) * 4);
+    try std.testing.expectApproxEqRel(@log2(@as(f32, 0.4)), result[3], comptime std.math.floatEps(f32) * 4);
 }

 test "@log10 f16" {
